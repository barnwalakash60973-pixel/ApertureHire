fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

pydantic>=2.8.0
pydantic-settings>=2.4.0

langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-google-genai>=2.0.0
langsmith>=0.1.147

python-docx>=1.1.0
pymupdf>=1.24.0

sqlalchemy>=2.0.30
aiosqlite>=0.20.0

bcrypt>=4.2.0
pyjwt>=2.9.0

# File storage (S3 backend) - optional at runtime (only imported if
# STORAGE_BACKEND=s3), but declared here so `pip install -r` covers it.
boto3>=1.34.0

# In-process scheduler for overdue-marking, reminder emails, and
# retention cleanup - replaces the old lazy-only "runs when HR loads a
# page" approach with a real periodic job.
apscheduler>=3.10.4
